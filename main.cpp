// Header-Dateien
#include "engine.h"

// Programmstart
int main() {
    Engine engine;  // Erzeugt ein Objekt der Klasse "Engine"
    engine.run();   // Aufruf der Methode "run()"
    return 0;       // Übergebe dem Betriebssystem den Integer 0 (erfolgreiches Programmende)
}

/*
[main.cpp] beinhaltet den Programmstart.
In dieser Datei wird die Methode "run()" aufgerufen, in welcher die Hauptschleife läuft.
*/
